---
type Props = {
  href: string
  label: string
  active?: boolean
}

const { href, label, active } = Astro.props
---

<li>
  <a
    class:list={[
      'group relative block w-5 aspect-square text-white text-base',
      {'hover:after:shadow-[inset_0_0_5px_1px]': !active },
      { 'after:shadow-[inset_0_0_0_5px]': active },
      "after:absolute after:inset-0 after:w-full after:aspect-square after:rounded-full after:border after:content-['']",
    ]}
    href={href}
  >
    <span
      class:list={[
        'absolute right-4 top-1/2 -translate-y-1/2',
        'text-base leading-none text-nowrap',
        'bg-background mr-4 p-2 rounded',
        'opacity-0 transition-opacity duration-500 group-hover:opacity-100',
      ]}
    >
      {label}
    </span>
  </a>
</li>
